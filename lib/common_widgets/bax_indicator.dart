import 'package:flutter/material.dart';

class BaxIndicator extends StatelessWidget {
  const BaxIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return const LinearProgressIndicator();
  }
}
